# Project Guidelines

- Keep application code under `src/app` and tests under `tests`.
- Preserve the API, domain, service, repository, and infrastructure boundaries.
- Use asynchronous FastAPI and SQLAlchemy APIs.
- Add focused tests for new behavior.
- Run `ruff check .` and `pytest` before submitting changes.
